name: Mirror to Public Repo

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  mirror:
    runs-on: ubuntu-latest
    steps:
      - name: Generate Token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: pub_personal_portfolio

      - name: Checkout Private Repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # CRITICAL: This prevents the local token from overriding your push token
          persist-credentials: false 

      - name: Push to Public Repo
        env:
          # Storing the token in an env variable for cleaner use
          APP_TOKEN: ${{ steps.generate_token.outputs.token }}
        run: |
          # Use the token explicitly in the URL
          git remote add public "https://x-access-token:${APP_TOKEN}@github.com/itsddpanda/pub_personal_portfolio.git"
          
          # Force push to the public repo
          git push public main --force
